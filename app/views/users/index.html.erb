<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <% if user.activated? %>
      <%= render user %>
    <% end %>
  <% end %>
</ul>

<%= will_paginate %>
